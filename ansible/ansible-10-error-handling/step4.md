# Шаг 4: Разбираем block/rescue/always

Повторно зафиксируем смысл тройки `block/rescue/always`, но теперь через краткий отчёт.

```bash
cat /root/ansible_rescue_result.txt /root/ansible_always_result.txt > /root/ansible_block_rescue_summary.txt
cat /root/ansible_block_rescue_summary.txt
```{{execute}}
