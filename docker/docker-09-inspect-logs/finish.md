## Урок пройден

Теперь у вас есть базовая связка для первичной диагностики контейнера:

- `docker inspect`
- `docker logs`
- `docker exec`

Это одна из самых частых комбинаций при разборе инцидентов в Docker.
