# Архитектура Kubernetes

Docker запускает контейнеры на одной машине. Kubernetes (**K8s**) — на сотнях машин сразу. Он решает, куда поставить контейнер, перезапускает упавшие и распределяет трафик.

> Кластер уже готов. Проверим: `kubectl get nodes`{{execute}}
