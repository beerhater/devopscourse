## Копируем файл

Команда `cp` (Copy) копирует файл.
Синтаксис: `cp откуда куда`

**Задание:**
Скопируйте `hello.txt` в новый файл `backup.txt`:

`cp hello.txt backup.txt`

Проверьте что оба файла существуют: `ls`
