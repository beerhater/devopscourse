## Создаём свою директорию

Команда `mkdir` (Make Directory) создаёт новую папку.

**Задание:**
Создайте директорию для вашего первого проекта:

`mkdir devops_project`

Проверьте что папка создалась командой `ls`.
