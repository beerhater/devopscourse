## Удаляем файл

Команда `rm` (Remove) удаляет файл. 

⚠️ **Важно:** в Linux нет корзины. Удалённый файл восстановить нельзя.

Чтобы удалить директорию со всем содержимым используется флаг `-rf`:
`rm -rf папка`

**Задание:**
Удалите файл `hello.txt`:

`rm hello.txt`

Убедитесь что файл исчез: `ls`
